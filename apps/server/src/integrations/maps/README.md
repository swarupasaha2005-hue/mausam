# maps integration

Superseded by `integrations/routing/osrm/` (Phase 7 spec named the
directory `routing`, not `maps`). Route/directions logic lives there;
this directory is kept empty/unused rather than duplicated.
